# API examples\n\nThis file shows how to call the `/evaluate-with-visualization` endpoint.\n\nExample (curl):\n\n```bash\ncurl -X POST http://localhost:8000/evaluate-with-visualization \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"problem_id\": \"quadratic_binary\", \"x\": [0.0, 0.0, 0.0, -2.0, 2.0, 0.0]}'\n```\n\nExample (Python requests):\n\n```python\nimport requests\nurl = \"http://localhost:8000/evaluate-with-visualization\"\npayload = {\"problem_id\": \"quadratic_binary\", \"x\": [0.0, 0.0, 0.0, -2.0, 2.0, 0.0]}\nr = requests.post(url, json=payload)\nprint(r.json())\n```\n\nThe response contains:\n- `y`: loss\n- `gradient`: analytical gradient\n- `accuracy`: optional if dataset is available\n- `points`: dataset points (for plotting)\n- `coefficients`: the coefficients you evaluated (same as request.x)\n+\n*** End Patch"}]}
